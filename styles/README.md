# styles/
CSS 模块化拆分 (将来可能拆分自 inline)

- `base/variables.css` CSS 变量 (蓝白配色 + 反色 + 暗夜)
- `base/reset.css` reset
- `base/layout.css` Hero / Section / Card 布局
- `base/typography.css` 字号字重
- `themes/apple-light.css` 默认日间
- `themes/apple-dark.css` 暗夜模式
