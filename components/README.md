# components/
可复用 HTML 片段。每个 .html 文件是一个独立 snippet, 可被各 pages 复制使用。

- `cards/` 各类卡片片段
- `quotes/` 重点引用 / 易错警示 / 记忆口诀
- `grids/` 多列网格布局
- `toc/` 左侧浮动目录
- `fab/` 右下角浮动按钮组
- `navigation/` 顶部按钮 / PPT 控件
