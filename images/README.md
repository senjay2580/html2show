# images/
主题相关插图。按主题分子目录。

- `_common/` 跨主题共用 (logo, banner 等)
- `deadlock/` 死锁主题用图
- `os/` 操作系统通用
- `ds/` 数据结构
- `network/` 计算机网络

引用方式:
```html
<img src="../images/deadlock/rag-cycle.png" alt="...">
```
